@model IEnumerable<MyRACIT.Models.Entities.Department>

@{
    ViewData["Title"] = "Управління кафедрами";
}

<div class="row mb-3">
    <div class="col">
        <h2>🏢 Управління кафедрами</h2>
    </div>
    <div class="col text-end">
        <a asp-action="CreateDepartment" class="btn btn-success">
            ➕ Додати кафедру
        </a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        <strong>ℹ️ Інформація:</strong> Кафедри відсутні. Додайте першу кафедру.
    </div>
}
else
{
    <div class="card">
        <div class="card-body">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th>№</th>
                        <th>Назва кафедри</th>
                        <th style="width: 150px;">Дії</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        var index = 1;
                    }
                    @foreach (var department in Model)
                    {
                        <tr>
                            <td>@index</td>
                            <td><strong>@department.Name</strong></td>
                            <td>
                                <a asp-action="EditDepartment" asp-route-id="@department.Id" 
                                   class="btn btn-sm btn-primary" title="Редагувати">
                                    ✏️
                                </a>
                                <button type="button" class="btn btn-sm btn-danger" 
                                        data-bs-toggle="modal" 
                                        data-bs-target="#deleteModal@(department.Id)"
                                        title="Видалити">
                                    🗑️
                                </button>
                            </td>
                        </tr>
                        
                        <!-- Delete Confirmation Modal -->
                        <div class="modal fade" id="deleteModal@(department.Id)" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header bg-danger text-white">
                                        <h5 class="modal-title">Підтвердження видалення</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p>Ви впевнені, що хочете видалити кафедру <strong>"@department.Name"</strong>?</p>
                                        <p class="text-danger"><small>⚠️ Увага: Ця дія незворотня!</small></p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Скасувати</button>
                                        <form asp-action="DeleteDepartment" asp-route-id="@department.Id" method="post" class="d-inline">
                                            <button type="submit" class="btn btn-danger">Так, видалити</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        index++;
                    }
                </tbody>
            </table>
        </div>
    </div>
    
    <div class="mt-3">
        <p class="text-muted">
            <small>Всього кафедр: <strong>@Model.Count()</strong></small>
        </p>
    </div>
}
